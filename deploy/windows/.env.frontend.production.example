# Keep empty for same-origin deployment with FastAPI static hosting.
VITE_API_BASE_URL=
VITE_FILE_BASE_URL=
# Optional: override LSP websocket endpoint.
# Leave empty to auto use ws(s)://<current-host>:3001
VITE_LSP_WS_URL=

